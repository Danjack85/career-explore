# T04 — 行业列表与详情

## 列表页（`src/views/IndustryList.vue`）

- 卡片显示：`name` / `stage` / `summary` / `jobs` / `updated_at`。
- 顶部筛选：**全部 / 萌发 / 成长 / 成熟 / 衰退 / 低谷观察**。
- 空状态友好（一句话 + 一个动作按钮）。

## 详情页（`src/views/IndustryDetail.vue`）

- 显示全部字段。
- **突出 `sources` 和 `source_date`**：来源与日期要明显，不能藏在角落。
- `risks` 用警示色 `#B45309`。
- `stage` 用阶段标签。

## 约束

- 行业结论必须带来源和日期；数据为示例时保持「示例数据，需人工替换」原文，不得删除该提示。
- 不新增字段，不改数据结构。

## 验收

- [ ] 10 条卡片全部渲染
- [ ] 6 个筛选项均可切换，筛选结果正确
- [ ] 详情页字段完整，来源与日期可见
- [ ] 空状态友好
- [ ] `pnpm lint && pnpm build` 通过

## 输出

变更摘要、测试结果、下一步建议。
