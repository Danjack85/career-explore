# T08 — 全流程联调

## 目标

全页面走一遍，修复 lint / build / 路由 / 数据问题，统一空状态和错误处理，**无阻塞**。

## 检查清单

- [ ] 首页 4 个入口 → 对应功能页，返回可回退
- [ ] 行业列表 → 筛选 → 详情 → 返回
- [ ] 测评 15 题 → 结果页 → 加入实验
- [ ] 信息雷达 → tag 筛选 → 打开来源链接
- [ ] 计划 → 编辑 → 保存 → 刷新 → 仍在
- [ ] 实验列表 → 详情 → 记录日志 → 复盘
- [ ] 底部导航切换正常，无死链
- [ ] 各页空状态统一（一句话 + 一个动作按钮）
- [ ] 错误处理：数据读取失败有兜底文案，不白屏
- [ ] 深色 / 窄屏下不溢出

## 验收

- [ ] `pnpm lint && pnpm build` 通过
- [ ] 上述清单全部通过
- [ ] 无阻塞项（有则写 `docs/BLOCKED.md`）

## 输出

变更摘要、测试结果、下一步建议。
